import { makeStyles } from '@material-ui/core'

const useStyle = makeStyles({
})


function Feed() {
  const classes = useStyle();
  return (
    <div>
        
    </div>
    

  );
}

export default Feed;
